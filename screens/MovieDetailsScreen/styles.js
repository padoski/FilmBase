import { StyleSheet, StatusBar } from "react-native";
const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",

    padding: 10,
    backgroundColor: "#fff",
  },
  inputContainer: {
    width: 320,
    alignItems: "center",
  },
  button: {
    width: 250,
    marginTop: 20,
  },
});
export default styles;
